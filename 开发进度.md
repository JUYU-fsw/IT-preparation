# MSPM0G3507 终版小车开发进度

> 建立日期：2026-07-23  
> 当前阶段：开发与烧录链路验证

## 1. 固定协作方式

从现在开始采用以下分工：

1. Codex 负责直接修改工程代码，并在修改后进行力所能及的静态核对。
2. 用户负责在 Keil 中编译、烧录和进行实物测试。
3. Codex 每次修改完成后，必须明确说明：
   - 修改了哪些文件；
   - 修改了什么功能；
   - 用户下一步需要怎样编译、烧录和测试；
   - 预期现象是什么。
4. 用户将 Keil 的编译信息、下载信息和实物测试现象反馈给 Codex。
5. Codex 根据反馈继续修改代码，并更新本进度文件。
6. 除非用户另有要求，不再让用户手工输入或修改代码。

## 2. 工程位置

- 终版工程根目录：`D:\ai embody\TI preparation\project\MSPM0G3507_Car`
- Keil 工程：`D:\ai embody\TI preparation\project\MSPM0G3507_Car\project\mdk\SeekFree_MSPM0G3507_Device_Library.uvprojx`
- 主程序：`D:\ai embody\TI preparation\project\MSPM0G3507_Car\project\user\src\main.c`
- 逐飞原仓库保持不修改。

## 3. 已完成

- 已核验 Keil、Arm Compiler 6、MSPM0 SDK、SysConfig 和 MSPM0 DFP 的安装情况。
- Windows 已识别 Horco CMSIS-DAP v2，并曾枚举出虚拟串口 COM10。
- 已将完整逐飞模板复制到本项目工作区：
  - 共 685 个文件；
  - 约 22.35 MB；
  - 保留 `libraries`、`project` 及其相对路径结构；
  - 抽查文件的 SHA-256 与逐飞源仓库一致。
- Keil Flash Download 当前配置已确认：
  - `Erase Sectors`；
  - `Program`；
  - `Verify`；
  - `Reset and Run`；
  - `MSPM0G MAIN 128KB`；
  - Flash 地址范围 `0x00000000`～`0x0001FFFF`；
  - 算法 RAM 为 `0x20200000`，大小 `0x00008000`。
- 已将 `main.c` 改为最小 LED 闪烁程序：
  - PA14 推挽输出；
  - 每 500 ms 翻转一次；
  - 保留系统时钟初始化和调试串口初始化。
- 第一次编译结果为 1 个错误、2 个警告：
  - 错误位于 `main.c`，`gpio_toggle()` 未声明；
  - 原因是该名称仅在定义 `COMPATIBLE_WITH_OLDER_VERSIONS` 时作为旧版兼容宏存在；
  - 已改用当前库正式接口 `gpio_toggle_level()`；
  - 两个警告来自逐飞库 `zf_driver_pit.c` 的枚举类型比较，暂不影响 LED 链路测试。
- 修正后已经成功编译、烧录，板载 LED 正常闪烁。由此确认：
  - PA14 确实能够控制板载 LED；
  - Arm Compiler 6 可以编译当前工程；
  - Horco CMSIS-DAP 能识别目标芯片；
  - SWD 擦除、编程、校验、复位和程序运行链路正常。
- 已进入调试串口验证阶段：
  - 使用逐飞模板的 `debug_init()`；
  - UART：UART0；
  - 波特率：115200；
  - 逐飞空模板最初默认 TX=PA10、RX=PA11；
  - 程序每秒输出一次 `MSPM0G3507 heartbeat: LED loop running.`；
  - PA14 LED 继续每 500 ms 翻转，作为程序运行心跳。
- 第一次串口测试现象：
  - COM10 可以正常打开和监听；
  - 没有收到串口文本；
  - 用户反馈 LED 仍然亮着。
- 已根据终版载板原理图修正调试串口引脚：
  - 原理图 UART0 TX 为 PA0；
  - 原理图 UART0 RX 为 PA1；
  - `zf_common_debug.h` 已改为 `UART0_TX_A0` 和 `UART0_RX_A1`；
  - 波特率保持 115200。
- 用户确认上一次测试没有连接串口 TX/RX；补充连接并重新烧录后，VS Code 串口监视器仍无输出。
- VS Code 串口监视器截图显示：
  - 端口为 COM10；
  - 波特率为 115200；
  - 端口能够正常打开；
  - 接收区域没有数据。
- 按用户要求进行第二轮诊断修改：
  - GPIO 心跳从 PA14 改到 PB16；
  - PB16 每 500 ms 翻转一次；
  - 串口仍为 UART0、TX=PA0、RX=PA1、115200；
  - 输出由 `printf()` 改为直接调用 `uart_write_string()`，绕过 printf 重定向；
  - 每秒发送 `MSPM0G3507 heartbeat: PB16 loop running.`。
- 第二轮测试结果：
  - PB16 LED 正常闪烁，证明程序主循环持续运行；
  - VS Code 监听 COM10 仍无任何串口数据；
  - 因为直接 UART 发送和主循环均正常，当前优先怀疑 COM10 与 PA0 之间的物理连接、TX/RX 方向或 Horco 虚拟串口接口。
- 独立 USB-TTL 测试结果：
  - USB-TTL 枚举为 COM9；
  - PB16 LED 仍正常闪烁；
  - UART 仍无输出；
  - 测试时曾同时连接 GND、5V、RX、TX，并尝试过同名连接和交叉连接。
- 供电安全结论：
  - 当前载板已经由核心板 USB 供电；
  - USB-TTL 的 5V 必须断开，禁止与载板 5V 相连，避免双电源并联及反向供电；
  - 单向日志测试只接载板 GND 与 USB-TTL GND，以及载板 TX 与 USB-TTL RX；
  - 若 USB-TTL 的 TX 是 5V 电平，不得直接接入 MSPM0 的 RX。
- 已渲染并目视核对原理图，载板顶部 UART0 四针接口从 1 到 4 为：
  - 1：+5V；
  - 2：GND；
  - 3：UART0_RX_PA1；
  - 4：UART0_TX_PA0。
- 第三轮诊断固件改为同时初始化并发送四路 UART：
  - UART0：TX=PA0，RX=PA1，发送 `UART0 PA0 OK`；
  - UART1：TX=PB6，RX=PB7，发送 `UART1 PB6 OK`；
  - UART2：TX=PA23，RX=PA24，发送 `UART2 PA23 OK`；
  - UART3：TX=PB2，RX=PB3，发送 `UART3 PB2 OK`；
  - 四路均为 115200，且每秒发送一次；
  - PB16 LED 心跳保持不变。
- 第三轮四路 UART 测试全部通过：
  - UART0 PA0 正常；
  - UART1 PB6 正常；
  - UART2 PA23 正常；
  - 载板丝印 UART4 对应的 PB2（芯片 UART3）正常；
  - USB-TTL 使用 COM9；
  - PB16 LED 持续正常闪烁。
- 已明确区分“载板接口丝印编号”和“MSPM0 内部 UART 外设编号”：
  - 载板 UART4 实际对应芯片 UART3，信号为 PB2/PB3；
  - 载板 UART3 与 UART2 共用 PA23/PA24，但 UART3 带 12V；
  - 载板 UART5 与 UART1 共用 PB6/PB7，但 UART5 带 12V；
  - UART3/UART5 是四线电机兼容接口，本车不使用。
- 用户已更正此前信息：实物 UART2 供电脚为 5V，与现有 PDF 一致；总档案已同步修正。
- 四路 UART 诊断代码已收敛：
  - 后续固定使用 UART0，TX=PA0、RX=PA1、115200；
  - UART1、UART2、UART3 不再由当前程序初始化；
  - PB16 继续作为运行心跳。
- 已进入蜂鸣器测试阶段：
  - 原理图控制信号为 PA7；
  - TMB12A05 资料确认是 5V 电磁式有源蜂鸣器；
  - 程序暂按高电平触发；
  - 每 2 秒输出一个约 200 ms 的短鸣脉冲；
  - UART0 同步输出蜂鸣器测试日志。
- 蜂鸣器测试通过，用户确认模块丝印明确标注“低电平触发”。
- 已修正蜂鸣器控制逻辑：
  - PA7 默认输出高电平关闭蜂鸣器；
  - 每 2 秒拉低约 200 ms 短鸣；
  - 不再使用此前的高电平触发测试逻辑。
- 已进入 OLED 测试阶段：
  - 原理图 OLED SCL=PA31、SDA=PA28；
  - 使用逐飞软件 I2C，避免后续与 MPU6050 硬件 I2C 资源冲突；
  - 暂按常见 SSD1306 地址 0x3C、128x64 分辨率初始化；
  - OLED 显存每 2 秒在 0xAA/0x55 棋盘格之间切换；
  - UART0 输出 OLED 初始化和刷新日志；
  - PB16 LED 与蜂鸣器测试继续保留。
- OLED 测试通过：
  - 屏幕成功显示密集横纹图案；
  - 确认 SSD1306 地址 0x3C、128x64 初始化和 PA31/PA28 软件 I2C 通信正常。
- 蜂鸣器低电平触发逻辑测试通过；后续改为仅启动时短鸣一次。
- 已进入 MPU6050 通信测试阶段：
  - 原理图 MPU6050 SCL=PA1、SDA=PA0、AD0 接地；
  - 软件 I2C 地址为 0x68；
  - PA0/PA1 与原 UART0 冲突，因此调试串口迁移到 UART1；
  - 新调试串口为 UART1，TX=PB6、RX=PB7、115200；
  - 程序退出 MPU6050 睡眠，并每秒读取寄存器 0x75 `WHO_AM_I`；
  - 正常预期值为 0x68；
  - OLED 保留静态横纹图案，PB16 继续闪烁。
- 实际读取到 `WHO_AM_I = 0x70`。
- 根据 TDK InvenSense 官方 MPU-6500 寄存器手册，0x70 是 MPU-6500 的默认设备 ID；I2C 地址仍为 0x68。
- 当前模块应按 MPU-6500 或其兼容芯片处理，不再按标准 MPU6050 的 0x68 设备 ID 判断。
- 已进入 IMU 原始数据测试：
  - 采样率配置约 125 Hz；
  - 数字低通滤波开启；
  - 加速度量程 ±2 g；
  - 陀螺仪量程 ±250 dps；
  - 每秒读取一次加速度 XYZ 和陀螺仪 XYZ 原始值；
  - UART1 输出格式为 `ACC[x,y,z] GYRO[x,y,z]`。
- 用户提供的 7 组静止原始数据统计：
  - 加速度均值约 `[671, 182, 15177]`；
  - 加速度标准差约 `[61, 21, 25]` LSB；
  - 陀螺仪均值约 `[-282, 41, -56]` LSB，即 `[-2.15, 0.31, -0.43] dps`；
  - 陀螺仪标准差约 `[0.034, 0.067, 0.050] dps`；
  - 当前主要问题是稳定零偏，不是严重随机噪声，暂不需要更换模块。
- 已加入上电自动零偏校准：
  - 上电后要求小车水平、静止 4 秒；
  - 按 125 Hz 采集 500 组样本；
  - 自动计算加速度 XYZ 和陀螺仪 XYZ 偏置；
  - 水平校准时将 Z 轴目标设为 16384 LSB（+1 g）；
  - 校准完成后蜂鸣器短鸣一次；
  - 串口输出偏置和校准后的 `CAL ACC[...] GYRO[...]`。
- 用户提供的校准后静止数据表现良好：
  - ACC X/Y 约在 ±60 LSB 内；
  - ACC Z 围绕 16384 小幅波动；
  - GYRO 最大约 15 LSB，折合约 0.11 dps；
  - 当前 IMU 满足小车定角转弯和航向辅助的基础要求，无需更换。
- OLED 测试界面已升级为状态文字界面，校准完成后显示：
  - `TI CAR READY`；
  - `IMU: MPU-6500`；
  - `CALIBRATION: OK`；
  - `UART1: 115200`。
- 用户提供动画素材：`D:\ai embody\TI preparation\小猫.mp4`。
- 素材参数：720x720、30 FPS、约 15.77 秒。
- 已制作 OLED 小猫动画：
  - 截取约 2 秒的连贯动作；
  - 转换为 128x64 单色画面；
  - 小猫主体缩放到约 60x60 并居中；
  - 共 12 帧，播放约 6 FPS；
  - 动画数据占用约 12 KB Flash；
  - 生成文件为 `project/code/cat_animation.h`。
- 新的 OLED 流程：
  - 上电期间显示测试图案；
  - IMU 校准完成后显示状态文字约 1.5 秒；
  - 随后循环播放小猫动画；
  - 动画播放期间 PB16 心跳和 IMU 串口数据仍继续运行。
- 小猫动画实机测试通过，播放流畅。
- 已将当前可直接编译烧录的完整娱乐版工程另存为：
  - `D:\ai embody\捂嘴喵\MSPM0G3507_Car`
  - Keil 入口：`D:\ai embody\捂嘴喵\MSPM0G3507_Car\project\mdk\SeekFree_MSPM0G3507_Device_Library.uvprojx`
  - 原始素材：`D:\ai embody\捂嘴喵\小猫.mp4`
  - 共 851 个文件，约 26.83 MB；
  - 已核对 `.uvprojx`、`main.c` 和 `cat_animation.h` 的 SHA-256，均与当前已验证版本一致。
- 八路灰度模块当前状态：
  - 尚未接入终版 MSPM0G3507 载板；
  - 该模块的巡线功能已经在初版四 TT 电机小车上通过实车验证；
  - 旧车验证结果可以复用扫描逻辑、黑线有效电平和误差计算思路；
  - 终版车仍需重新验证载板引脚、选通时序、左右顺序、安装高度和实际赛道参数。
- 正式主工程已退出小猫动画播放；娱乐版仍完整保存在 `D:\ai embody\捂嘴喵`。
- 已建立独立 TB6612 双电机底层：
  - 新增 `project/code/tb6612.c` 和 `tb6612.h`；
  - PWMA=PA12/TIMG0_C0；
  - AIN1=PB19、AIN2=PB17；
  - PWMB=PA13/TIMG0_C1；
  - BIN1=PA16、BIN2=PB24；
  - PWM 频率暂定 20 kHz；
  - 有符号占空比接口范围为 -10000～10000；
  - 正数和负数控制两个方向，0 为滑行停止；
  - 当前 `TB6612_OUTPUT_ENABLE=0`，编译期安全锁生效，任何速度命令都会被强制为 0；
  - 主程序上电初始化 TB6612 后保持双通道 PWM=0；
  - OLED 正式状态界面显示 `MOTOR: LOCKED`；
  - Keil 工程文件已加入 TB6612 源文件。
- TB6612 安全基线实机验证通过：
  - PB16 正常闪烁；
  - OLED 正常显示；
  - 电机输出仍保持锁定。
- 已建立双编码器软件正交解码模块：
  - 新增 `wheel_encoder.c/.h` 并加入 Keil 工程；
  - MOTOR1：A=PA25、B=PA14（旧档案曾误记为PA24/PA25，已纠正）；
  - MOTOR2：A=PA26、B=PA27；
  - 四根信号均使用 GPIO 双边沿中断；
  - 使用 16 项正交状态转移表进行四倍频有符号累计；
  - 当前无需主电源，可在 USB 供电下手动转动车轮测试；
  - UART1 每秒输出 `ENC MOTOR1=... MOTOR2=...`；
  - OLED 显示 `ENCODER: READY`；
  - 尚未定义 MOTOR1/MOTOR2 与实际左/右轮的对应关系和正方向，必须通过实物测试确认。
- 首次双编码器手动测试结果：
  - MOTOR1 计数只能在 0 和 1 之间变化，无法连续累计；
  - 该现象高度符合 A/B 两相中只有一相产生边沿的情况；
  - MOTOR2 计数正常；
  - MOTOR2 对应电机向小车前进方向转动时计数减小，反向转动时计数增大；
  - MOTOR1 实际左右轮映射和正方向仍未确认。
- 已加入编码器逐相信号诊断：
  - 分别累计 MOTOR1 A、MOTOR1 B、MOTOR2 A、MOTOR2 B 的中断边沿数；
  - 同时输出两路当前 AB 电平；
  - UART 格式为 `EDGE M1[A=... B=... AB=..] M2[A=... B=... AB=..]`；
  - 用于区分编码器本体/线束故障与载板接口/MCU引脚故障。
- 逐相诊断实测结果：
  - `M1[A=0 B=12271 AB=11]`；
  - `M2[A=4882 B=4881 AB=00]`；
  - 按旧错误映射监听时，PA25有信号而PA24没有信号；
  - MOTOR2 A/B 两相边沿数几乎完全一致，证明软件正交解码逻辑正常；
  - 该现象后来通过重新核对原理图确认是引脚映射错误，并非据此证明硬件损坏。
- 交换两个完整 6Pin 电机插头后，零边沿现象仍固定在软件所监听的 PA24，初步看似载板问题。
- 重新直接目视核对 2026-06-13 原理图后发现旧档案引脚记录有误：
  - MOTOR1 实际编码器信号为 PA25/PA14，不是 PA24/PA25；
  - MOTOR2 为 PA26/PA27；
  - 之前 PA24 始终为0、PA25持续产生边沿，正好符合“PA24未连接、PA25为有效编码器相”的实际情况；
  - 当前不应判定载板损坏。
- 已将 MOTOR1 软件正交解码改为 A=PA25、B=PA14，待重新实测。
- 两只电机的编码器方向已按统一机械视角确认：
  - 观察/摆放基准为电机位于内侧、车轮位于左侧；
  - 在该基准下，两只电机正转时编码器计数均减小，反转时计数均增大；
  - 该结论描述的是单体电机机械视角，不能直接等同于装车后的“向前”为正或为负；
  - MOTOR1/MOTOR2 与整车左轮/右轮的最终映射，以及装车向前时各自的计数符号，仍需在底盘安装后确认。
- 新增 STM32 麦轮 PID 参考工程：
  - 路径：`D:\robotmaster pj\RM PID\F401_PID 7.16\F401_PID-DIR\F401_PID-DIR`；
  - 重点文件：`User/app_task.c`、`bsp_encoder.c/.h`、`drv_tb6612.c/.h`、`Direction.c/.h`；
  - 该工程使用 100Hz 速度采样、四电机独立 PID、目标换向时清空 PID 状态、目标为0时停止输出；
  - 编码器参数写为电机轴每圈11脉冲、AB四倍频、减速比21.3，因此其输出轴理论计数为 `11×4×21.3=937.2`；
  - 上述21.3减速比只能作为当前 JGB680 的待验证线索，不能直接写入正式参数；
  - 可借鉴速度计算公式、独立控制器结构和换向清积分思路；STM32 HAL/定时器映射、四麦轮运动学、PID参数不能直接移植；
  - 源码含单轮1.855/0.98经验倍率、停止目标写0.01、直接使用浮点CMSIS PID等项目特例，正式工程不照搬。
- 输出轴每圈计数建议用轮胎与固定支架双基准线测量；连续手转5或10圈后用总计数差除以圈数，可显著降低人工停位误差。
- 当前两只 JGB680 输出轴十圈计数实测：
  - MOTOR1：起始 `-5252`，十圈后 `19461`，变化 `+24713`，即 `+2471.3 count/rev`；
  - MOTOR2：起始 `386`，十圈后 `-24371`，变化 `-24757`，即 `-2475.7 count/rev`；
  - 两路绝对每圈计数的平均值为 `2473.5 count/rev`，相差44计数/十圈，约0.18%，一致性良好；
  - 若编码器确为11脉冲/电机轴转且软件采用AB四倍频，则推算减速比约为 `2473.5/(11×4)=56.22:1`，很可能是标称56:1版本，仍需客服确认；
  - 本次两路转动方向相反，因此计数一正一负；计数绝对值用于每圈标定。
- 学长已将 STM32 麦轮项目配套电机一并提供，可在必要时作为整套替换电机使用；若确认其确为11PPR、21.3:1版本，可以连同其编码器换算参数作为起点，但仍需核对额定电压、电流、输出轴/安装尺寸及 TB6612 电流余量。
- 曾准备双电机一次性正反转测试流程，但复核档案后确认当前无法给电机安全供电：
  - 主电源开关尚未到货/安装，3S 电池到 12V、板载降压及 5V 网络的完整链路尚未验证；
  - 已立即恢复 `TB6612_OUTPUT_ENABLE=0`，任何测试命令都会被强制为 PWM=0；
  - 正反转测试代码暂时保留但被安全锁完全屏蔽，不得在供电链路验证前解除；
  - 本轮没有生成或烧录解除安全锁的新固件，实物仍未受到电机驱动。

## 4. 当前需要用户执行

1. 将 USB-TTL 调试线从载板 UART0 移到 UART1：
   - 载板 UART1 第 2 针 GND → USB-TTL GND；
   - 载板 UART1 第 4 针 PB6/TX → USB-TTL RX；
   - UART1 第 1 针 5V 不接；
   - UART1 第 3 针 PB7/RX 不接；
   - USB-TTL TX 不接。
2. 在 Keil 中 Rebuild，确认 0 Error 后烧录 MPU6050 测试固件。
3. USB-TTL 仍使用 COM9、115200、8N1、无流控。
4. 当前载板由核心板 USB 供电，USB-TTL 不接 5V。
5. 上电后的前 4 秒必须让小车水平、完全静止。
6. 校准结束时蜂鸣器短鸣一次，PB16 LED随后持续闪烁。
7. 串口应输出 `BIAS ACC[...] GYRO[...]` 和 `IMU calibration complete.`。
8. 静止时观察校准后的数据是否接近 `CAL ACC[0,0,16384] GYRO[0,0,0]`。
9. 分别倾斜和转动小车，确认加速度和陀螺仪数值会明显变化。
10. 确认 OLED 在校准完成后从横纹切换为四行英文状态文字。
11. 确认状态文字显示约 1.5 秒后，OLED 循环播放小猫动画，动作方向和画面无明显错位。
12. 重新编译正式工程，确认 TB6612 模块无编译错误。
13. 烧录后确认 OLED 显示 `MOTOR: LOCKED`、PB16 闪烁、IMU 日志正常，且当前未接主电源时不会尝试驱动电机。
14. 确认两只电机 6Pin 插头按丝印接入 MOTOR1/MOTOR2。
15. 仅使用核心板 USB 供电，不连接 3S 电池。
16. 分别只转动一只车轮，确认串口只有对应 MOTOR1 或 MOTOR2 计数变化。
17. 记录每只轮向车头前进方向转动时计数是增加还是减少。
18. 重新烧录逐相诊断固件，分别转动两只轮，记录四个 A/B 边沿累计值。
19. MOTOR1 正常时 A/B 边沿数应同时持续增加且数量接近；若其中一路保持0或极小，即可定位缺失相。
20. 将两个电机插头恢复到原来的 MOTOR1/MOTOR2 位置。
21. 重新编译并烧录 PA25/PA14 修正版。
22. 分别转动两只轮，确认 MOTOR1 和 MOTOR2 的 A/B 边沿数都同时增加且相互接近。

## 5. 本轮预期结果

- 编译结果为 `0 Error(s)`。
- Flash 下载及校验成功。
- PB16 上的测试 LED 保持约 1 秒的完整闪烁周期。
- 串口工具每秒收到一行 `MSPM0G3507 heartbeat: PB16 loop running.`。

## 6. 待确认事项

- Horco CMSIS-DAP 的虚拟串口是否已经通过物理线路连接载板 PA0/PA1。
- 当前所用 Horco 线束是否包含虚拟串口 TX、RX 和 GND，还是只连接了 SWD。

## 7. 后续顺序

LED 和下载链路验证成功后，依次进行：

1. 调试串口周期输出；
2. 主电源路径安全检查；
3. 蜂鸣器、OLED 和 MPU6050 单模块验证；
4. 架空测试左右电机；
5. 验证左右编码器及计数方向；
6. 实现左右轮速度闭环；
7. 接入八路灰度并实现基础循迹。
