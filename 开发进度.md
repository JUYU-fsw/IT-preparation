# MSPM0G3507 终版小车开发进度

> 更新日期：2026-07-26
> 排序规则：里程碑与进度按日期升序；待办按优先级排序。

## 固定协作方式

1. Codex 负责直接修改工程代码，并在修改后进行可执行的静态核对。
2. 用户负责在 Keil 中编译、烧录和进行实物测试，无需手工输入或修改代码。
3. Codex 每次修改后说明：改动文件、实现功能、测试步骤与预期现象。
4. 用户反馈 Keil 编译/下载信息及实物现象，Codex 据此继续修改代码并同步更新文档。
5. 除非用户另有要求，正式工程与阶段测试副本保持隔离，实验性代码不直接覆盖安全基线。

## 项目位置

- **工作区根目录**：`D:\ai embody\TI preparation`
- **终版工程根目录**：`D:\ai embody\TI preparation\project\MSPM0G3507_Car`
- **终版 Keil 工程**：`D:\ai embody\TI preparation\project\MSPM0G3507_Car\project\mdk\SeekFree_MSPM0G3507_Device_Library.uvprojx`
- **终版主程序**：`D:\ai embody\TI preparation\project\MSPM0G3507_Car\project\user\src\main.c`
- **当前 PID 测试副本**：`D:\ai embody\TI preparation\before the final car\MSPM0G3507_Car`
- **PID 测试 Keil 工程**：`D:\ai embody\TI preparation\before the final car\MSPM0G3507_Car\project\mdk\SeekFree_MSPM0G3507_Device_Library.uvprojx`
- **娱乐版归档**：`D:\ai embody\捂嘴喵\MSPM0G3507_Car`
- **PCB 接线与引脚速查**：`D:\ai embody\TI preparation\PCB接线与引脚速查.md`
- **工程原则**：逐飞原仓库保持不修改，所有正式开发均在本工作区副本中进行。

## 项目总览

终版双轮小车已完成工具链、板载外设、双编码器、20～100 RPM 架空闭环、HC-05 双向通信以及八路灰度自适应巡线；2026-07-26 用户实车确认当前巡线效果完美，基础循迹阶段验收通过。

## 项目开发路径

**烧录与 LED 验证 ✅ → UART 调试 ✅ → 蜂鸣器 ✅ → OLED ✅ → IMU 识别与校准 ✅ → TB6612/编码器 ✅ → 20～100 RPM 架空闭环 ✅ → HC-05 双向启停 ✅ → 八路灰度接入 ✅ → 自适应巡线与急弯/丢线处理 ✅ → 速度提升与整车联调 ← 当前**

- **当前位置**：外层工程使用商家 `2450 count/rev`；HC-05 发送 `1` 后等待 3 秒并持续巡线，发送 `0` 可立即停车。当前巡线稳定，下一步按每次 5 RPM 的幅度提升直线速度并复测全赛道。

## 当前有效参数

| 类别 | 当前参数 | 状态/用途 |
|---|---|---|
| 自有电机 | 56:1，178 RPM，额定 1 A、堵转 2 A | 最终车参数 |
| 电机转矩 | 额定 6.5 N·m，最大 9 N·m | 用户已确认 |
| 编码器 | AB 两相，每电机轴 11 周期，MCU 四倍频 | 理论 `2464 count/rev` |
| 闭环换算 | `2450 count/rev` | 按用户决定采用商家数据，不使用 2473.5 |
| 调试串口 | UART1，PB6/PB7，115200-8N1 | USB-TTL 单向日志 |
| 控制周期 | 灰度/巡线基础循环 10 ms，测速/PID 窗口 20 ms | 为提高弯道执行响应已缩短 |
| 速度滤波 | 一阶低通 `alpha=0.25` | 配合 20 ms 速度环 |
| 速度 PID 当前值 | M1=`10/1.0/0`，M2=`10/1.0/0`，输出限幅 8000 | 20 ms 增量式 PI 参数 |
| 起步补偿 | `START_DUTY=1000` | 自有电机需重新标定 |
| 巡线速度 | 直线 55 RPM，普通弯最低 20 RPM | 弯道立即减速，直线渐进恢复 |
| 急弯速度 | 外轮 28 RPM，内轮 -12 RPM | 最外侧连续命中 20 ms 后进入 |
| 丢线恢复 | 外轮 22 RPM，内轮 -10 RPM，搜索 200 ms | 超时后双轮停车 |
| 电机输出 | 外层工程 `TB6612_OUTPUT_ENABLE=1` | 供电和实车巡线均已验证 |

## 阶段验收标准

- **供电链路完成**：3S、12 V、板载 5 V 和共地关系确认；烧录器不向目标板重复供电，无异常发热或压降。
- **电机方向完成**：MOTOR1/2 左右映射、正 PWM 方向及整车前进时编码器符号全部记录且一致。
- **速度闭环完成**：两轮架空下可稳定跟踪 20/30/60 RPM，无持续反向、无长期 `±3000` 饱和，并记录稳态误差。
- **PID 调参完成**：架空与落地负载下均能平稳起步、加减速和停车，两轮速度差满足直行需求。
- **循迹完成**：八路灰度左右顺序和阈值确认，基础赛道上可连续循线且不频繁丢线。

## 最近可用基线

- **正式安全基线**：`project\MSPM0G3507_Car`；TB6612 输出锁定，OLED、IMU、UART1、PB16 心跳及编码器底层可用。
- **PID 预验证基线**：`before the final car\MSPM0G3507_Car`；2026-07-24 使用 Arm Compiler 6.24 编译，`0 Error(s), 0 Warning(s)`。
- **编译规模**：Code=26408、RO-data=2524、RW-data=212、ZI-data=5516；该结果对应学长车参数，不代表自有电机 PID 已验收。

## 里程碑节点

- **2026-07-23｜工程与烧录链路打通**：复制并校验逐飞模板，Arm Compiler 6 编译、CMSIS-DAP 烧录、PA14 LED 闪烁均通过。
- **2026-07-23｜四路 UART 实机验证**：UART0/1/2/3 的 PA0、PB6、PA23、PB2 发送均通过，正式调试口固定为 UART1 PB6/PB7、115200。
- **2026-07-23｜板载外设验证完成**：PA7 低电平蜂鸣器、SSD1306 128×64 OLED（0x3C，PA31/PA28 软件 I2C）实测通过。
- **2026-07-23｜IMU 识别与校准完成**：确认器件为 MPU-6500 兼容芯片（WHO_AM_I=0x70），500 点上电零偏校准后静态数据满足定角与航向辅助需求。
- **2026-07-23｜娱乐版归档**：OLED 小猫动画实机通过并独立保存；正式主工程已移除动画，回归控制功能开发。
- **2026-07-23｜电机与编码器底层建立**：完成 TB6612 双路驱动和双编码器四倍频解码，纠正 MOTOR1 引脚为 PA25/PA14。
- **2026-07-23｜自有电机编码器标定**：两路输出轴平均 `2473.5 count/rev`，一致性误差约 0.18%；后续确认电机规格为 56:1、178 RPM。
- **2026-07-24｜学长车 PID 预验证完成**：在独立副本完成 50 ms 测速、低通滤波、双路独立 P 控制与起步补偿，编译为 0 错误 0 警告。
- **2026-07-24｜准备切换自有电机**：正式参数将由学长车 `937.2 count/rev` 改为自有电机 `2473.5 count/rev`，随后重测方向、死区和低速闭环。
- **2026-07-25｜外层工程切换自有电机 PID**：按用户决定直接修改 `project\MSPM0G3507_Car`，闭环换算固定采用商家 `2450 count/rev`，不再采用学长车参数或 2473.5 实测均值；加入双路 50 ms 增量式 PID、速度低通、起步补偿和编码器方向宏，并解除 TB6612 编译期输出锁。IMU 校准、每秒数据输出及 PA7 校准完成短鸣均保留；Arm Compiler 6.24 全量编译结果为 `0 Error(s), 0 Warning(s)`。
- **2026-07-25｜自有电机首轮方向测试**：确认左轮为 MOTOR2、右轮为 MOTOR1；原程序正目标下 MOTOR2 前进、MOTOR1 后退，且 MOTOR2 前进反馈为负。由此确定整车正目标对应 MOTOR1 驱动符号 -1、MOTOR2 +1；首轮反馈符号设置随后由第二轮完整正反转日志复核并修正。首轮无剧烈抖动，修正前稳态约为 M1 20 RPM、M2 13～15 RPM，不能用于 PID 增益结论。
- **2026-07-25｜自有电机第二轮方向测试**：机械方向确认正确；正目标时两轮均驱动车身前进，负目标时均后退。日志进一步确认两路反馈均与整车坐标反号，因此最终设为 MOTOR1 编码器符号 -1、MOTOR2 +1。修正前在约 1520 duty 下两轮均稳定在 22～23 RPM、无明显抖动；加入 `KI=0.5`，用于消除目标 30 RPM 时约 7 RPM 的稳态误差。
- **2026-07-25｜左右轮独立 PI 与停用 IMU**：用户实测共用 `KI=1.5` 时 M2 从 28.75 RPM 持续超调至 34.92 RPM并剧烈振荡，而 M1 加速仍慢，且出现 duty 增加但速度下降。PID 改为独立参数：M1=`KP 10/KI 1.5/KD 0`，M2=`KP 10/KI 0.5/KD 0`；PID 测试阶段停止 MPU-6500 初始化、校准、采样及 CAL/GYRO 日志，保留 PA7 启动短鸣。
- **2026-07-25｜发现闭环通道交叉配对**：日志显示 M1 duty 从 1627 增至 3000 时 M1 由 16.97 降至 7.16 RPM，而 M2 duty 从 1193 降至 585 时 M2 由 23.77 升至 48.97 RPM，证明两个 PID 控制量施加到了对方车轮，并非 KP/KI/KD 整定问题。确认 TB6612 A 对应编码器 MOTOR2、TB6612 B 对应编码器 MOTOR1；已交换输出配对，并将两路 KI 暂退回 0.5 验证。
- **2026-07-25｜30 RPM 架空闭环验收**：通道配对修正后，M1/M2 分别稳定到 29.77/29.68 RPM，目标误差约 0.77%/1.07%，两轮差 0.09 RPM；两路 `d50=61`，与 2450 CPR 下 30 RPM 的理论 61.25 count/50 ms 一致，duty 约 1908/1869且无饱和。该阶段验收通过。
- **2026-07-25｜进入多速度测试**：自动程序改为 3 秒待机后依次运行 20、30、60、100 RPM，每档 8 秒、档间停车 2 秒，末尾停车 2 秒后循环。因 30 RPM 已需约 1900 duty，测试限幅由 3000 提高至 8000，确保 60/100 RPM 档有足够输出余量；100 RPM 仍仅允许架空测试。
- **2026-07-25｜20～100 RPM 多速度架空验收**：用户确认 20/30/60/100 RPM 各档运行效果均非常稳定，最高 duty 不到 6000；说明当前双轮 PI、2450 CPR 换算和交叉通道配对在该速度范围内工作正常。相对 8000 软件限幅仍至少保留约 2000 duty 调节余量，可进入落地负载与实际线速度标定。
- **2026-07-25｜轮径与 HC-05 落地测试准备**：用户提供轮胎直径 6.6 cm，对应理论周长约 20.73 cm；20 RPM 理论线速度约 6.91 cm/s。HC-05 规划接板载 UART4（实际 MCU UART3，TX=PB2、RX=PB3、+5V、GND），无线波特率 9600；UART1 115200 有线日志继续保留，两路同步输出。程序改为一次性落地安全测试：等待 3 秒、20 RPM 前进 5 秒、随后永久停车，理论行程约 34.6 cm。
- **2026-07-25｜蓝牙启停与左右轮实物映射修正**：上电保持停车；HC-05 收到字符 `1` 后等待 3 秒，再按用户当前设置以 60 RPM 前进 5 秒并自动停车，收到字符 `0` 时立即清 PID 并停止双电机。此前 `START/STOP` 仍保留兼容，但考虑9600波特率连续字符串可能在10 ms轮询间隔内超过较小的UART接收FIFO，正式测试改用单字节 `1/0`。UTF-8下数字和英文ASCII编码不变，不影响本命令。用户实物确认左轮接 MOTOR1编码器，其中B相为PA14；最终映射为左轮=MOTOR1(PA25/PA14)+TB6612 B，右轮=MOTOR2(PA26/PA27)+TB6612 A。
- **2026-07-25｜HC-05 双向通信实机通过**：接线 `VCC→+5V、GND→GND、RXD→PB2、TXD→PB3`；电脑可持续接收 PID 日志，发送单字符 `1` 能收到 ACK 并触发测试，发送 `0` 可立即停车。UART3(PB2/PB3) 9600 baud 的收发链路均已确认，UTF-8 对单字节数字命令无影响。
- **2026-07-26｜八路灰度巡线接入**：YB-MVX05 按地址选通方式扫描，默认接线为 A0=PB25、A1=PB18、A2=PB21、OUT=PB22；黑线有效为高电平。使用八路加权位置误差、PD 转向外环和双轮速度 PI，蓝牙 `1` 等待 3 秒后持续巡线，`0` 立即停车。
- **2026-07-26｜弯道控制优化并实车验收**：针对固定 55 RPM 和 `base±correction` 导致外轮在急弯加速的问题，改为按误差及误差变化率动态降速、外轮不超过自适应基础速度、内轮可停止或反转；增加最外侧连续确认的急弯状态、中心稳定退出、按最后误差方向搜索 200 ms 及超时停车。速度环由 50 ms 提高到 20 ms，KI 按周期缩放为 1.0、滤波改为 0.25。用户实车反馈“现在巡线完美”，基础巡线验收通过。

## 核心问题与最终方案汇总

### 已验证模块简表

- **基础链路**：Keil/Arm Compiler 6、CMSIS-DAP 烧录、LED 心跳正常。
- **调试串口**：UART1，TX=PB6、RX=PB7、115200。
- **蜂鸣器**：PA7，低电平触发；正式流程仅启动完成时短鸣。
- **OLED**：SSD1306 128×64、地址 0x3C，PA31/PA28 软件 I2C。
- **IMU**：MPU-6500 兼容芯片，I2C 地址 0x68、设备 ID 0x70；上电静止采集 500 点完成零偏校准。

### 电机驱动-安全锁与供电

- 【问题】主电源开关及 3S→12V/5V 完整链路未验证时，电机测试存在反供电和失控风险｜【根因】烧录器、USB-TTL、外部电源可能同时驱动同一电源网｜【最终方案】供电链路现已具备测试条件，外层工程允许 TB6612 输出；外部供电时烧录器只接 GND/SWDIO/SWCLK/NRST/VTref，关闭或断开 target power，并确保共地。

### 编码器-缺相假象与计数标定

- 【问题】MOTOR1 计数仅在 0/1 间变化，逐相诊断显示一路始终为 0，曾疑似载板损坏｜【根因】旧档案将 MOTOR1 错记为 PA24/PA25，实际为 PA25/PA14，PA24 本就未连接｜【最终方案】修正为 MOTOR1 A=PA25、B=PA14；MOTOR2 A=PA26、B=PA27，继续使用双边沿中断和 16 项状态表四倍频累计。
- 【问题】自有电机减速比及速度换算参数不确定｜【根因】学长车 `937.2 count/rev/21.3:1` 不适用于自有电机｜【最终方案】已确认 56:1、178 RPM；闭环统一采用商家给出的 `2450 count/rev`。理论值 2464 和十圈实测均值 2473.5 仅作为历史核对数据，不再参与程序换算。

### 速度闭环-振荡与起步死区

- 【问题】低速目标附近 PWM 在 `±3000` 饱和翻转、速度正负跳变｜【根因】10 ms 窗口计数颗粒过粗、直接照搬 `35/10` 增益过大，且两路编码器原始符号不同｜【最终方案】测速/PID 窗口改为 50 ms，加入 `alpha=0.35` 低通并分别校正符号；预验证参数为 `KP=10, KI=KD=0, limit=3000`。
- 【问题】`KP=1`、目标 30 RPM 时电机不启动｜【根因】约 30 duty 的纯 P 输出低于机械静摩擦死区｜【最终方案】输出采用“目标方向起步占空比 + PID 修正”，预验证 `START_DUTY=1000`；换自有电机后以 600/800/1000/1200 开环档重新标定两轮死区。

## 关键技术决策记录

- **工程位置切换**：学长车 PID 预验证仍保留在 `before the final car` 作为历史副本；自有电机后续开发按用户要求直接在外层 `project` 工程进行。
- **调试串口选 UART1**：PA0/PA1 留给 MPU-6500 I2C，UART1 PB6/PB7 已通过物理发送测试且无资源冲突。
- **编码器采用 GPIO 四倍频解码**：四根输入均用双边沿中断与状态转移表，能获得有符号计数并诊断单相缺失。
- **正式巡线速度环使用 20 ms 窗口**：20～100 RPM 架空验收阶段曾使用 50 ms；接入巡线后为缩短弯道执行延迟改为 20 ms，并同步将 KI 从 2.5 缩放为 1.0、滤波系数改为 0.25。
- **巡线采用自适应速度与状态机组合**：普通弯按位置误差及变化率降速，外轮限速；最外侧连续命中进入急弯差速/反转，丢线按最后方向短时搜索，搜索超时停车。
- **不直接移植 STM32 PID 参数**：仅复用独立控制器、换向清状态和速度换算思路；HAL 映射、麦轮运动学及经验倍率均不适配本车。
- **外层工程已允许电机输出**：开关与供电链路就绪后，`TB6612_OUTPUT_ENABLE` 已改为 1；首次烧录必须架空两轮，并随时准备断电。
- **编码器按 5V 供电设计**：厂家推荐编码器供电 5V；接入 MCU 前须确认 AB 输出电平或板载调理电路与 MSPM0 GPIO 兼容。

## 当前待办与风险

- **已完成｜多速度架空测试**：20/30/60/100 RPM 均通过，最高 duty 不到 6000。
- **已完成｜HC-05 双向串口**：电脑接收日志及 `1/0` 无线启停均已实机通过。
- **已完成｜八路灰度基础巡线**：地址扫描、加权误差、动态弯道降速、急弯模式和丢线恢复均已接入，用户实车确认巡线完美。
- **P0｜逐步提升巡线速度**：仅先调整 `line_follow.h` 中 `LINE_FOLLOW_STRAIGHT_RPM`，建议从 55 RPM 开始每次增加 5 RPM，全赛道复测后再继续；当前其他弯道参数保持不动。
- **P0｜校准轮胎滚动周长**：当前直径 6.6 cm 仅作理论换算；按常用负载直线滚动 10 圈并测总距离，左右轮最好分别测量，单位 mm。
- **P1｜落地与负载测试**：在空载平整地面和常用负载下复测，验证阻力变化时 PI 能否维持相同轮速。
- **P1｜建立线速度与路程换算**：取得轮胎滚动周长和轮距后，将 RPM 转换为 cm/s，并基于编码器累计计数实现距离控制、减速和停车。
- **已完成｜八路灰度接入终版载板**：使用 H55 的 PB25/PB18/PB21/PB22，安装预瞄距离约 7～8 cm，实车方向、扫描和巡线均已验证。
- **P1｜核对驱动电流余量**：电机额定电流 1 A、堵转电流 2 A；需结合 TB6612 单通道持续/峰值能力、散热和堵转保护判断是否安全。
- **已确认｜电机机械参数**：减速比 56:1、额定转速 178 RPM、额定转矩 6.5 N·m、最大转矩 9 N·m。
- **P2｜确认编码器电平兼容性**：编码器推荐 5 V 供电，需确认 AB 输出是否经过载板降压、开漏上拉或具备 3.3 V 兼容高电平。
- **风险｜供电冲突**：USB-TTL 5V 禁止接入已由核心板 USB 供电的载板；不确定 TTL TX 电平时，不接入 MCU RX。
- **风险｜文档编号混淆**：载板 UART4 对应芯片 UART3；带 12V 的 UART3/UART5 为电机兼容口，本车不使用。
