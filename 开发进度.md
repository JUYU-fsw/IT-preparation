# MSPM0G3507 终版小车开发进度

> 更新日期：2026-07-26
> 本文件只记录当前有效状态、已完成里程碑和真正未完成事项；历史排错过程见《调试日志.md》。

## 项目位置

- 工作区：`D:\ai embody\TI preparation`
- 正式工程：`D:\ai embody\TI preparation\project\MSPM0G3507_Car`
- Keil 工程：`project\MSPM0G3507_Car\project\mdk\SeekFree_MSPM0G3507_Device_Library.uvprojx`
- 主程序：`project\MSPM0G3507_Car\project\user\src\main.c`
- `before the final car` 仅为学长车 PID 历史测试副本，不是当前开发基线。

## 当前项目状态

此小车准备用于参加电赛，因此在分析时可结合往年考题。终版双轮小车的实车固件已经完成主控工具链、烧录、板载外设、双电机驱动、双编码器、速度闭环、HC-05 双向通信、YB-MVX05 八路灰度接入以及自适应巡线。用户已完成实车验证并确认巡线效果完美。

## 项目开发路径

**烧录与 LED ✅ → UART 调试 ✅ → 蜂鸣器 ✅ → OLED ✅ → IMU 识别与校准 ✅ → TB6612 与供电 ✅ → 双编码器解码与标定 ✅ → 自有电机速度闭环 ✅ → HC-05 双向启停 ✅ → 八路灰度接入 ✅ → 自适应巡线 ✅ → 弯道/急弯/丢线处理 ✅ → MPU6050 Yaw角度环+里程计 ⚠️ → 实车测试与整定 ← 当前**

- 实车不是“等待接入灰度”或“等待基础循迹”阶段；这两项已经完成。
- 当前主线是保持已验收巡线基线，按赛题增加功能；若需要提速，每次只把直线速度提高 5 RPM 并复测全赛道。

## 当前硬件映射

| 功能 | 当前有效映射 |
|---|---|
| 左轮 | MOTOR1 编码器 PA25/PA14 + TB6612 B |
| 右轮 | MOTOR2 编码器 PA26/PA27 + TB6612 A |
| 电机 PWM/方向 | A：PA12/PB19/PB17；B：PA13/PA16/PB24 |
| 调试串口 | UART1，PB6/PB7，115200 |
| HC-05 | 板载 UART4（MCU UART3），PB2/PB3，9600 |
| OLED | SSD1306，PA31/PA28 软件 I2C，地址 0x3C |
| 蜂鸣器 | PA7，低电平触发 |
| 灰度地址 A0/A1/A2 | PB25/PB18/PB21 |
| 灰度数字输出 OUT | PB22 |

灰度模块使用 H55-1～H55-4，黑线有效为高电平；软件约定 bit0 为最左侧、bit7 为最右侧。传感器阵列位于驱动轮轴线前约 7～8 cm，该安装距离已通过实车验证。

## 已验收实车固件参数

| 类别 | 当前参数 |
|---|---|
| 电机 | 56:1，178 RPM，额定电流 1 A、堵转电流 2 A |
| 编码器换算 | `2450 count/rev` |
| 基础循环 | 10 ms |
| 测速/速度 PI 周期 | 20 ms |
| 双轮速度 PI | KP=10、KI=1.0、KD=0 |
| 速度滤波 | `alpha=0.25` |
| 输出限幅 | 8000 |
| 起步补偿 | 1000 |
| 直线巡线速度 | 55 RPM |
| 普通弯最低速度 | 20 RPM |
| 急弯 | 外轮 28 RPM、内轮 -12 RPM |
| 丢线搜索 | 外轮 22 RPM、内轮 -10 RPM，最多 200 ms |

## 新增：MPU6050 Yaw角度环参数

| 类别 | 参数 |
|---|---|
| 模块文件 | `mpu6050_yaw.c/h`, `angle_pid.c/h`, `odometer.c/h` |
| IMU型号 | MPU-6500 (WHO_AM_I=0x70), I2C 0x68 |
| 陀螺仪量程 | ±2000 dps |
| Yaw积分方式 | 独立梯形积分（dt 由 TIM_G12 微秒自由计数器实时测量） |
| 死区滤波 | ±0.25 dps |
| 角度环模式 | WEAK(巡线叠加) / STRONG(脱线保持) |
| WEAK PID | KP=0.5, KI=0.01, KD=0.1, 输出限幅±15 RPM |
| STRONG PID | KP=2.0, KI=0.05, KD=0.3, 输出限幅±55 RPM |
| 巡线叠加系数 | 0.25（WEAK模式） |
| 脱线叠加系数 | 1.00（STRONG模式，全权重接管） |
| 里程计 | 编码器累计 → cm, 车轮66mm, 2450 count/rev |
| 蓝牙命令 | 1=START, 0=STOP, r=重置Yaw归零, d=查询里程 |
| 停车抗扰测试 | `h`锁定当前航向，±1.5°死区，最大±18 RPM；`0`立即退出 |

### ⚠️ 当前验证项：倾斜安装 Yaw 投影修正待实车复测

- **原现象**：旋转 90° 示数约 75~80°，旋转一圈示数约 300°，比例约为 0.83。
- **已确认根因**：程序只积分传感器自身 `GYRO_Z`，但实车 MPU 固定倾斜安装；车体绕竖直轴旋转时，角速度在传感器 Z 轴上的分量会按夹角余弦缩小。固定比例与实测约 0.83 相符。
- **2026-07-26 修正**：
  1. 校准由“必须水平”改为“只需保持静止”，采集三轴陀螺零偏和静止重力方向。
  2. 运行时读取三轴陀螺，将去零偏后的角速度向量投影到校准重力轴，再进行 Yaw 积分。
  3. 校准样本恢复为 500 点；启动日志增加 `IMU_AXIS`，周期日志增加投影角速度和三轴角速度。
  4. Keil Arm Compiler 6.24 全量编译通过：`0 Error(s), 0 Warning(s)`。
- **第二轮实测**：手转约 90°仅显示约70°，说明仍按约10ms积分而实际循环更长；360°后回绕显示约55°，原日志无法分辨未回绕累计量。
- **第二个已确认根因**：逐飞 `timer_init(TIM_G12, TIMER_US)` 已用 `CPS=79` 将80MHz预分频为1MHz，但 `timer_get()` 对 TIM_G12 又将硬件计数除以80。航向代码拿到的伪周期小于500μs后一直回退到固定10ms。
- **第二轮修正**：航向模块直接读取已为1MHz的 `TIM_G12->CTR`；日志增加 `total` 未回绕角度和 `dt` 实测周期。再次全量编译通过，`0 Error(s), 0 Warning(s)`。
- **待验证**：不接电机主电源，整车保持原安装姿态，依次手转 ±90°及分段 360°，确认误差是否进入 ±3°。
- **当前分支**：`angle_loop`（`Car_angle` worktree），未合并到 main。

## 控制架构

```
10ms主循环:
  1. 蓝牙命令 (1/0/r/d)
  2. 灰度扫描
  3. MPU6050 Yaw快速更新 (gyro-only, ~0.15ms)
  4. 巡线更新 → 产生base RPM目标
  5. 角度环PID更新 → 产生diff RPM修正
  6. 角度修正叠加:
     有黑线: WEAK模式 0.25系数叠加
     脱线:   STRONG模式全权重接管航向保持
  7. 每20ms: 速度PI + 里程更新 + 电机输出
  8. 每500ms: LED心跳 + OLED刷新
  9. 每1s: 串口日志 (含yaw/angle_corr/odo)
```

## 已验收实车巡线行为

1. 上电保持停车；HC-05 发送 `1` 后等待 3 秒并持续巡线，发送 `0` 立即停车。
2. 八路灰度以 `-350～350` 权重计算位置误差。
3. 普通弯按误差和误差变化率立即降低基础速度；回正后逐渐恢复速度。
4. 外轮不会因转向修正而超过当前基础速度；内轮可减速、停止或反转。
5. 最外侧连续命中 20 ms 后进入急弯状态，中心通道稳定 30 ms 后退出。
6. 丢线后沿最后一次误差方向搜索 200 ms，仍未找到才停车。
7. 串口日志输出 `mode/mask/error/base/correction/target/rpm`。

## 已完成里程碑

- **2026-07-23**：Keil/Arm Compiler 6、CMSIS-DAP、LED、UART、蜂鸣器和 OLED 实机通过。
- **2026-07-23**：MPU-6500 兼容芯片识别与零偏校准完成。
- **2026-07-23**：TB6612 底层和双编码器四倍频解码完成；MOTOR1 引脚纠正为 PA25/PA14。
- **2026-07-23**：自有电机十圈标定平均约 2473.5 count/rev；正式闭环按用户决定采用商家 2450 count/rev。
- **2026-07-25**：左右轮、驱动通道和编码器反馈符号完成实车校正。
- **2026-07-25**：30 RPM 闭环误差约 1%；20/30/60/100 RPM 架空测试均稳定，最高 duty 不到 6000。
- **2026-07-25**：HC-05 PB2/PB3 双向通信及 `1/0` 启停实机通过。
- **2026-07-26**：YB-MVX05 接入终版载板，完成八路扫描、加权位置误差和巡线外环。
- **2026-07-26**：完成动态弯道降速、外轮限速、急弯状态机和丢线恢复；用户实车确认巡线完美。
- **2026-07-26**：完成 MPU6050/MPU6500 兼容 Yaw 模块、角度 PID 和编码器里程计。针对传感器倾斜安装，改为校准三轴零偏与重力方向并投影角速度；采用安装标定系数 1.268 后，手动 90°显示 89.7°。详细日志转移到 UART1 后 HC-05 恢复稳定。
- **2026-07-26**：停车抗扰测试时确认 IMU 已无响应：`WHO_AM_I=FF`、三轴全零，拆下后明显发烫。固件已加入失败重试、OLED 故障提示及禁用角度保持保护；最新编译 0 error、0 warning。
- **2026-07-26**：巡线直线速度 70 RPM 已由用户确认稳定，作为当前基线。

## 当前待办与风险

以下只保留尚未完成或长期存在的事项：

- **P0｜更换 IMU 并重新验收测角**：购买同针序 GY-521，驱动兼容 `WHO_AM_I=0x68/0x70`；先验供电和温升，再完成静止、±90°及分段 360°标定。
- **P0｜停车抗扰与行驶航向保持**：新 IMU 测角通过后继续 `h` 低速测试，先确认电机修正方向，再整定回正速度、超调和扰动恢复。
- **P0｜自动直角转弯与定位**：实现灰度路口触发、唯一方向判定、90°角度闭环、重新捕线和编码器定距停车；目前状态机尚未整车验收。
- **P1｜巡线速度基线**：当前 70 RPM 已稳定；如需提速，每次只增加 5 RPM 并复测完整赛道。
- **P1｜核对 TB6612 电流与温升余量**：电机额定 1 A、堵转 2 A；长期高负载运行时继续观察驱动器、电机和导线温升，禁止人为堵转。
- **P2｜确认编码器 5 V 输出电平裕量**：当前实机工作正常，但仍建议核对 AB 输出是否经过板载调理或符合 MSPM0 GPIO 电平要求。
- **风险｜供电冲突**：载板已有主电源或核心板 USB 供电时，不连接 USB-TTL 的 5 V；烧录器和载板必须共地。
- **风险｜接口编号混淆**：板载 UART4 实际对应 MCU UART3（PB2/PB3）；带 12 V 的 UART3/UART5 不作为普通串口使用。

## 后续改动原则

- 已验收的巡线参数作为当前安全基线，不同时大幅修改速度、PD、急弯和丢线参数。
- 只提速时优先修改 `LINE_FOLLOW_STRAIGHT_RPM`。
- 修改灰度板安装方向或线序后，必须重新确认 bit0～bit7 的左右顺序。
- 修改速度 PI 周期后必须同步重新整定 KI 和滤波参数。
- 新的实车结论需同步更新本文件、总档案、接线速查和调试日志。
