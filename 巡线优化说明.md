# 八路数字巡线鲁棒性优化说明

## 适用硬件与修改边界

- 灰度模块为 YB-MVX05，当前接口只有 A0/A1/A2 选通和一路数字 OUT。
- 程序只能获得 8 路黑/白状态，不能获得模拟灰度值。本次优化针对二值数据完成，不使用不存在的 ADC 数据。
- 保留原有 GPIO 接线、蓝牙 `1/0` 启停、3 秒安全等待、双编码器、双轮速度 PI、TB6612 接口、急弯和丢线搜索架构。
- 正常黑线约连续触发 2 路；当前赛道只考虑直角与圆弧，不把全黑或宽黑区当作合法赛道元素。

## 修改文件

### `line_sensor.h` / `line_sensor.c`

1. 保存原始 `raw_mask`，便于判断干扰来自传感器还是算法。
2. 对每一路做 3 帧多数表决，滤除只持续 1 帧的反光和纹理脉冲。
3. 将滤波结果拆成连续黑色区域。存在多个区域时，优先选择较宽区域；宽度相同时选择更接近上一位置的区域，避免远处孤立黑点拉偏误差。
4. 使用选中连续区域的加权质心计算误差，权重仍为 `-350,-250,...,350`，保持原方向和量程。
5. 增加线路状态与可信度：
   - `0 LOST`：没有黑点；
   - `1 NORMAL`：1～3 个连续黑点；
   - `2 WIDE`：超过 3 个连续黑点；
   - `3 ALL_BLACK`：八路全黑；
   - `4 NOISY`：存在多个不连续黑块。
6. 正常连续 2 点可信度为 100；单点/3 点为 80；多块或宽黑区降低可信度，以触发控制层降速。

### `line_follow.h` / `line_follow.c`

1. 直线最高速度由 70 RPM 提升为 100 RPM，但只在误差小、变化小、可信度高时逐步恢复。
2. 对位置误差增加一阶低通，对误差微分再次低通并限幅，降低二值位置跳变对 PD 的冲击。
3. 基础速度同时参考滤波误差、滤波微分和线路可信度：
   - 弯道或异常立即降速；
   - 回到直线后每 10 ms 只恢复 1 RPM，避免刚出弯立即冲高。
4. 普通控制仍限制外轮不超过基础速度，只降低或反转内轮。
5. 最外侧连续触发 2 帧才进入直角模式；中心重新稳定 3 帧才退出。
6. 丢线时按最后有效误差方向搜索，最长 20 个控制周期（约 200 ms），之后停车。
7. 全黑经过传感器 3 帧滤波确认后，只允许 3 个周期、18 RPM 的短暂直行；持续全黑立即停车。

### `main.c`

1. START 和 STOP 时重置巡线控制器，避免上一次运行的误差、微分或丢线计数影响下一次启动。
2. OLED 每 100 ms 只刷新一行，四行约 400 ms 更新一轮，避免一次刷新整屏造成明显控制延迟。
3. UART 每秒输出一次完整诊断数据。长诊断只发送到 115200 UART1，避免 9600 HC-05 阻塞控制循环；蓝牙启停命令和 ACK 保持不变。

## OLED 字段

OLED 四行含义：

```text
R:xx F:xx S:xx
E:xxxx FE:xxxx Q:xxx
M:x ST:x W:x N:x
B:xxx C:xxxx
```

- `R`：原始八路位图；
- `F`：3 帧滤波后的位图；
- `S`：最终选中的连续线路区域；
- `E`：选中区域的原始加权位置误差；
- `FE`：送入方向 PD 的滤波误差；
- `Q`：线路可信度 0～100；
- `M`：巡线模式，0正常、1急左、2急右、3丢线搜索、4全黑短暂保持、5超时停车；
- `ST`：传感器状态，编号见上文；
- `W`：选中区域宽度（全黑时为 8）；
- `N`：滤波位图中的连续区域数量；
- `B`：当前动态基础速度 RPM；
- `C`：方向修正量 RPM。

## 主要可调参数

传感器参数集中在 `line_sensor.h`：

- `LINE_SENSOR_FILTER_SAMPLES`：多数滤波窗口，当前为 3；
- `LINE_SENSOR_NORMAL_MAX_WIDTH`：正常线路最大连续宽度，当前为 3。

方向和速度参数集中在 `line_follow.h`：

- `LINE_FOLLOW_STRAIGHT_RPM`：直线最高速度 100 RPM；
- `LINE_FOLLOW_MIN_CURVE_RPM`：普通弯最低基础速度 32 RPM；
- `LINE_FOLLOW_KP/KD`：方向 PD；
- `LINE_FOLLOW_ERROR_FILTER_ALPHA`：位置滤波系数；
- `LINE_FOLLOW_D_FILTER_ALPHA`：微分滤波系数；
- `LINE_FOLLOW_SHARP_*`：直角确认、退出及轮速；
- `LINE_FOLLOW_LOST_*`：丢线搜索时间和轮速；
- `LINE_FOLLOW_ALL_BLACK_*`：全黑短暂容错时间和速度。

## 首次实车验证顺序

1. 架空车轮，不启动巡线，移动黑胶带观察 OLED：
   - 直线中央应接近 `S=18`、`E=0`、`Q=100`；
   - 单帧遮挡或反光只应出现在 `R`，不应立即进入 `F/S`；
   - 两处不连续黑块应显示 `ST=4`、`N>1`。
2. 地面低风险测试时先把 `LINE_FOLLOW_STRAIGHT_RPM` 临时设回 70，确认滤波、直角和丢线行为没有退化。
3. 依次测试 80、90、100 RPM，不建议直接在狭小场地首次运行 100 RPM。
4. 重点记录入弯前的 `FE/derr/B/C` 和左右实际 RPM。如果直角冲出，先降低直角确认周期或提高误差降速强度，不要先盲目增大 KD。
5. 测试人工移除黑线：应搜索约 200 ms 后停车。
6. 测试八路同时遮黑：短暂慢行后停车，不应持续冲出。

## 当前编译结果

- Keil MDK / Arm Compiler 6.24 构建成功。
- 修改代码为 0 Error。
- 完整 Rebuild 出现 2 个逐飞库原有枚举比较警告，位置在 `zf_driver_pit.c:173`，与本次巡线修改无关。
