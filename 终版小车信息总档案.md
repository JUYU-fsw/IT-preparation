# MSPM0G3507 终版电赛小车信息总档案

> 建档日期：2026-07-22  
> 最近更新：2026-07-26（历年小车题与2026器件清单导向的发展规划）  
> 当前状态：两轮编码器基础平台和白底数字灰度巡线已完成；旧数字模块无法适应复杂背景，正切换 AO1～AO8 模拟灰度。项目下一阶段转向精确运动底座、通用任务框架和清单内扩展接口。

## 1. 项目目标和范围

- 用途：电赛基础小车平台，后续根据赛题少量增加模块、修改 PID 参数及巡线逻辑。
- 第一阶段项目均已完成：
  - MSPM0G3507 工程建立、编译和烧录；
  - 双电机驱动与方向校正；
  - 左右编码器读取；
  - 左右轮速度闭环；
  - 八路灰度读取；
  - 基础循迹与实车整定；
  - OLED 状态显示；
  - 蜂鸣器状态提示；
  - MPU6050 通信、校准，并按任务需要用于定角或航向辅助；
  - USB 转 TTL 调试输出。
- MaixCAM 视觉由队内另一名成员负责；本车侧需要准备稳定的供电、安装和 UART 数据接口。
- 不在赛题明确前制作专用避障、坡道或机械机构，但底盘需要具备对应的标准感知与执行扩展接口。
- 赛道规格：尚未确定。程序结构需要便于赛题公布后替换巡线逻辑。
- 工作原则：先稳定跑通，学习作为伴随目标；每天整理进度、接线、代码、测试结果、故障和下一步任务。

## 2. 主控和载板

- 主控：MSPM0G3507 核心板，插在排母上，可拆卸。
- 载板：`PCB/TI板卡`，现有原理图日期为 2026-06-13。
- 下载器：Horco CMSIS-DAP。
- 开发环境、逐飞 MSPM0G3507 开源库和终版小车专用 Keil 工程均已建立并完成编译、烧录和实机运行。
- 开发路线已经确定：Keil MDK + Arm Compiler 6 + TI MSPM0 SDK/SysConfig + MSPM0 DFP + Horco CMSIS-DAP，以逐飞库的 MDK 工程和例程作为竞赛开发框架。
- Horco CMSIS-DAP v2 的 SWD 识别、编译、烧录和运行链路已经通过 LED 及后续整车固件验证。

### 2.1 已安装开发环境

核验日期：2026-07-23。

| 项目 | 当前版本或状态 | 安装位置 |
|---|---|---|
| Keil MDK / µVision | 5.43.1 | `D:\software\keil5` |
| 旧 Keil 备份 | 5.24a，仅作安装器自动备份 | `D:\software\keil5\Backup.001` |
| Arm Compiler 6 | `armclang.exe` 文件版本 20.0.0git | `D:\software\keil5\ARM\ARMCLANG` |
| Arm Compiler 5 | 5.06，保留供旧工程使用 | `D:\software\keil5\ARM\ARMCC` |
| MSPM0 SDK | 2.11.00.07 | `D:\software\TI\mspm0_sdk_2_11_00_07` |
| SysConfig | 1.26.2+4477 | `D:\software\TI\sysconfig` |
| MSPM0 Keil DFP | `MSPM0G1X0X_G3X0X_DFP` 1.3.1 | `D:\software\keil5\ARM\PACK\TexasInstruments\MSPM0G1X0X_G3X0X_DFP\1.3.1` |
| 下载器 | Horco CMSIS-DAP v2；曾枚举 COM10 | USB 设备 |
| Git | 已安装 | `D:\software\Git` |
| VS Code | 已安装，仅作为编辑器/辅助工具 | `D:\software\Microsoft VS Code` |

DFP 已验证包含 MSPM0G350X SVD、128KB MAIN Flash 算法和 NONMAIN Flash 算法，Keil Pack Installer 中能够检索并选择 MSPM0G3507。

当前用户环境变量 `MSPM0_SDK` 和 `SYSCONFIG` 尚未持久设置。逐飞原生工程不一定依赖这两个变量，但使用速度闭环参考工程的一键配置脚本时，需要重新设置并完成 SysConfig 生成验证。SDK 2.11.00.07 要求 SysConfig 不低于 1.26.0，当前 1.26.2 满足要求。

### 2.2 逐飞 MSPM0G3507 开源库

- 本地路径：`D:\embodied materials\MSPM0G3507_Library`
- 远程仓库：`https://gitee.com/seekfree/MSPM0G3507_Library.git`
- 当前分支：`master`
- 核验提交：`a9a11b3ecfa3b8adf5819e25382ee56f8d35b8b1`
- 主模板工程：`SeekFree_MSPM0G3507_Opensource_Library\project\mdk\SeekFree_MSPM0G3507_Device_Library.uvprojx`
- 逐飞说明要求 MDK 5.37 及以上；当前 Keil 5.43.1 满足要求，并支持 DAP v2。
- 库中已有 GPIO、UART、ADC、PWM、PIT、外部中断、定时器、编码器、电机、OLED、IMU、无线通信等 MDK 例程。
- 建立终版工程时应复制模板或最接近的例程到本项目工作区，不直接在逐飞 Git 原仓库中开发。

### 2.3 原理图中的电机控制信号

| 功能 | MSPM0G3507 引脚 |
|---|---|
| PWMA | PA12 |
| AIN1 | PB19 |
| AIN2 | PB17 |
| PWMB | PA13 |
| BIN1 | PA16 |
| BIN2 | PB24 |
| STBY | 原理图接 +5V |

### 2.4 原理图中的编码器信号

| 电机接口 | 编码器信号 |
|---|---|
| MOTOR1 | PA25、PA14 |
| MOTOR2 | PA26、PA27 |

实车最终映射：左轮=MOTOR1(PA25/PA14)+TB6612 B，右轮=MOTOR2(PA26/PA27)+TB6612 A。

### 2.5 载板 UART 接口编号与实际信号

核验日期：2026-07-23。已对照现有原理图、`Gerber2` 中 10 份打板文件的器件信息和实物测试结果。

载板上印刷的 `UART0`～`UART5` 是物理接口名称，不一定等于 MSPM0G3507 内部 UART 外设编号。当前实物测试确认的信号关系如下：

| 载板丝印 | MSPM0 外设与信号 | 当前用途 |
|---|---|---|
| UART0 | UART0，TX=PA0，RX=PA1 | 可用于 USB-TTL 调试 |
| UART1 | UART1，TX=PB6，RX=PB7 | 可用备用串口 |
| UART2 | UART2，TX=PA23，RX=PA24 | 可用备用串口；供电脚为 5V |
| UART3 | 与 UART2 共用 PA23/PA24，但带 12V 供电脚 | 为可能使用的四线电机预留，本车不用 |
| UART4 | UART3，TX=PB2，RX=PB3 | 可用备用串口；注意接口名 UART4 对应芯片 UART3 |
| UART5 | 与 UART1 共用 PB6/PB7，但带 12V 供电脚 | 为可能使用的四线电机预留，本车不用 |

重要注意事项：

1. 用户已复核并更正：实物 UART2 的供电脚为 5V，与现有原理图一致。
2. 串口信号映射已通过四路固件和 USB-TTL 实测：PA0、PB6、PA23、PB2 均能正常发送。
3. UART3 和 UART5 带 12V，不作为普通 USB-TTL 调试口使用，也不纳入本车方案。
4. 后续接线必须优先依据用户实物丝印，并在连接供电脚前用万用表确认电压；不得只依据接口名称或旧版 PDF。
5. 当载板已经通过核心板 USB 或主电源供电时，USB-TTL 只接 GND、TX/RX 信号，不连接 USB-TTL 的 5V。
6. 普通单向日志接线为：载板 TX → USB-TTL RX、载板 GND → USB-TTL GND；USB-TTL TX 和电源脚可以悬空。

## 3. 电机、编码器和车轮

- 电机：JGB680/MG680 编码器减速电机 ×2，两只完全相同。
- 额定电压：12V。
- 电机外径：约 37mm。
- 输出轴：6mm D 型轴。
- 编码器：AB 相增量式方波输出。
- 编码器基础脉冲：电机转子每圈 11 脉冲。
- 输出轴单路基础脉冲数：`11 × 实际减速比`。
- 编码器供电范围：3.3～5V；当前 PCB 电机接口提供 +5V。
- 已确认减速比 56:1、额定转速 178 RPM、额定电流 1 A、堵转电流 2 A；正式闭环采用商家 `2450 count/rev`。
- 配套车轮：实测/当前采用直径约 66mm，6mm D 轴连接件。
- 两个 6Pin 电机插头能够插入 PCB 的 MOTOR1/MOTOR2 接口，用户确认线序一致；首次上电前仍要按接口丝印逐根复核。

### 3.1 六线定义

| 颜色 | 定义 |
|---|---|
| 红 | 电机 + |
| 白 | 电机 - |
| 蓝 | 编码器 VCC |
| 黑 | 编码器 GND |
| 黄 | 编码器 A 相 |
| 绿 | 编码器 B 相 |

交换红、白可以改变电机旋转方向。不要通过交换编码器电源线改变方向。

### 3.2 驱动兼容性风险

TI PCB 使用一颗 TB6612，A/B 两通道分别驱动一只电机。TB6612 官方输出能力为每通道平均 1.2A，3.2A 只允许作为很短的峰值。JGB680 的启动及堵转电流未知，因此目前不能认定驱动电流一定具有足够余量。

正式测试要求：

1. 首次测试架空车轮；
2. 从较低 PWM 开始，并使用斜坡逐渐增加占空比；
3. 每次测试时间保持很短，观察 TB6612、电机及导线温度；
4. 禁止人为堵转车轮来测试；
5. 若出现难以起步、驱动反复保护、主控复位或 TB6612 快速发热，立即断电并改用更大电流驱动器；
6. 后续优先向卖家取得堵转电流，或使用带限流功能的电源测量。

## 4. 底盘

- 底盘：DIY 双轮差速底盘，已经完成装配和实车巡线。
- 结构：两只驱动轮 + 前后两个牛眼轮。
- 底盘已装配；轮距和轴距尚未记录，当前仅明确灰度板预瞄距离约 7～8 cm。
- 灰度板位置：车头前方，位于驱动轮轴线前约 7～8 cm；当前安装位置已通过实车巡线验证。
- 四点支撑需要检查是否发生前后牛眼轮高度不一致造成驱动轮悬空或接地压力不足；必要时用垫片调整高度。

## 5. 电池和供电

- 电池：3S 锂聚合物航模电池。
- 标称电压：11.1V。
- 满电电压：12.6V。
- 容量：2200mAh。
- 当前实测电压：11.2V。
- 接口：XT60。
- 充电器：iMaxRC B3 PRO Compact Charger，支持 2S/3S 平衡充电。
- 主电源开关和整车供电链路已经投入实车运行。
- 板载可调降压模块：焊死在 PCB 上。
- 其余 LM2596：留作备用。
- 四节 AA 电池盒：是否使用根据后续需求决定，默认不纳入终版主供电。

### 5.1 当前供电状态

以下完整主供电路径已经在电机闭环和实车巡线中投入运行：

`3S 电池 → XT60 → 电源开关 → 12V 网络 → 板载降压模块 → +5V`

仍需遵守供电隔离：载板已有主电源或核心板 USB 供电时，不连接 USB-TTL 的 5V；烧录器只保留信号、地和电压检测连接，避免多个电源同时驱动同一网络。

### 5.2 主电源首次上电前检查

1. 确认开关焊盘实际数量和连通方式；若 PCB 是三焊盘转换开关位，两脚开关必须接公共端和其中一个有效端；
2. 断开核心板和所有可插拔模块；
3. 检查 12V-GND、5V-GND 是否短路；
4. 接入开关后做断电通断测试；
5. 首次仅接 3S 电池，测量 12V 网络；
6. 空载调节并确认降压输出为 5.0V 左右；
7. 确认极性和电压正确后再逐个插回核心板、OLED、MPU6050等模块。

## 6. 已确定的外设

| 模块 | 当前决定 |
|---|---|
| YB-MVX05-V1.0 八路数字灰度 | 已接入 H55：A0=PB25、A1=PB18、A2=PB21、OUT=PB22；纯白底黑胶带巡线通过，但复杂背景实测失败，确认是二值信息不足，后续仅保留为回退模块 |
| AO1～AO8 模拟灰度模块 | 已决定采购；到货后先验证 5 V 供电时 AO 最高电压，再确定 3.3 V 直连或八路分压及 ADC 映射 |
| OLED | 使用，显示速度、灰度误差、PID参数和故障状态；不单独显示“上电” |
| IMU 模块（原按 MPU6050/GY-521 记录） | 实测 WHO_AM_I=0x70，按 MPU-6500 或兼容芯片处理；SCL=PA1、SDA=PA0、AD0接地/I2C地址0x68；通信、原始数据和上电零偏校准均通过，校准后静止 GYRO 波动不超过约 0.11dps，满足当前小车用途，无需更换 |
| TMB12A05 蜂鸣器 | 已装；确认是 5V 有源蜂鸣器，PA7 控制，低电平触发，实测正常 |
| LED | PB16 已用于程序心跳状态 |
| MaixCAM | 暂缓，由队友调试 |
| HC-05 | 已接板载 UART4（MCU UART3 PB2/PB3），9600 baud，日志和 `1/0` 启停实机通过 |
| USB 转 TTL | 已有，优先用于第一阶段串口日志 |

终版 MSPM0G3507 已完成旧模块 X1～X8 地址扫描、黑线高电平识别、左右顺序确认、加权误差、动态速度巡线、急弯和丢线恢复。纯白底黑胶带条件下 70 RPM 实车效果良好；复杂背景、反光和纹理环境下旧模块本身不能可靠区分黑线，软件滤波不能恢复未输出的模拟反射率信息。

## 7. 建议的软件和调试结构

### 7.1 控制层次

1. 电机 PWM 和方向底层；
2. 左右编码器计数及速度计算；
3. 左右轮独立速度 PID；
4. 八路灰度扫描及加权位置误差；
5. 巡线外环产生左右目标速度；
6. 丢线、启动失败、编码器异常和超时保护；
7. MPU6050 仅在基础闭环稳定后加入定角转弯或航向保持。

### 7.2 调参方式顺序

1. 程序内固定参数：用于最初单模块测试；
2. USB 转 TTL：输出编码器、灰度和 PID 数据，是第一阶段主要调试手段；
3. HC-05：当前用于无线日志和 `1/0` 启停；速度等参数仍通过源代码调整；
4. 按键：用于启动、停止和模式切换；
5. OLED：用于低频状态显示，不承担高速日志输出。

### 7.3 速度闭环参考工程的复用结论

参考资料：

`D:\embodied materials\keil5环境配置\环境配置压缩包\速度闭环\My_M0_Proj\M0_Templant_FreeRTOS`

该目录是一个带 Git 历史的 MSPM0G3507 + FreeRTOS 工程，当前分支为 `speed-only`，核验提交为 `836a6860a7bef9ff3e807e23cb37f51dbd251549`。它有较高参考价值，但不能整套不经核对直接替换终版工程。

可直接借鉴的部分：

- `Component/control/speed_loop_core.*`：速度环纯算法与硬件层分离；10ms 采样、50ms 控制窗口、RPM 整数换算、一阶低通、目标斜坡、增量式 PID 和低速启动前馈。
- `Component/pid/pid.*`：增量式 PID 整数实现和“先累加、再限幅”的正确限幅逻辑。
- `Component/service/speed_service.*`：编码器、控制算法和 TB6612 之间的硬件适配层。
- `Component/tb6612/*`：双电机方向、PWM、制动接口的模块化写法。
- `Component/encoder/*`：硬件 QEI 与 GPIO 软件正交解码并用、16 位计数器溢出处理、累计计数和增量快照接口。
- `Component/task/*` 和 `FreeRTOSConfig.h`：任务、队列、通知以及控制周期组织方法。
- `Component/UART/*`：UART0 日志和重定向。
- `Component/oled/*`：软件 I2C OLED，可规避 OLED 与 MPU6050 同占 I2C0 外设的冲突。
- `main.syscfg`、`ti_msp_dl_config.*`：80MHz 时钟、PWM、QEI、UART、I2C、GPIO 和 10ms 定时器配置方式。
- `setup_mspm0_env.bat`、`scripts\setup_mspm0_env.ps1`、`keil\syscfg.bat`：自动修补本机 SDK include/lib 路径并在 Keil Build 前生成 SysConfig 代码的思路。
- `docs\工程总结.md` 和 `docs\工程分享与环境配置手册.md`：环境迁移、速度环/yaw 环调试、ISR 与任务分工、故障排查和工程分享清单。

必须适配或重新验证的部分：

1. 参考工程的编码器引脚为硬件 QEI `PA26/PA27`，另一路 GPIO 软件解码 `PA25/PA14`；重新目视核对终版 PCB 原理图并结合逐相实测后，确认终版载板同样是 MOTOR1 `PA25/PA14`、MOTOR2 `PA26/PA27`。旧档案曾误记为 MOTOR1 `PA24/PA25`，现已纠正。
2. 参考工程的电机方向引脚名称与终版原理图存在 AIN1/AIN2 顺序差异；最终以原理图表格和实车正转测试为准。
3. 参考工程写有 `ENCODER_COUNTS_PER_REV = 440`，并在部分注释中另述 30:1 减速比和 13200 计数，不适用于当前已确认 56:1 的 JGB680；正式程序采用商家 `2450 count/rev`。
4. 参考工程的 PID、前馈、斜坡和 yaw 参数来自另一套电机、底盘、负载和地面，只能作为结构参考，终版车必须低速重新整定。
5. `main.syscfg` 原始元数据是 MSPM0 SDK 2.04.00.06 + SysConfig 1.23.1；分享脚本/文档后来按 SDK 2.10.00.04 + SysConfig 1.26.2 验证。当前机器是 SDK 2.11.00.07 + SysConfig 1.26.2，需重新运行生成、编译和烧录验证。
6. 文档中包含 MPU6050 DMP、yaw 角闭环、PB21 目标角切换等总结，但当前 `speed-only` 分支的实际源文件应逐项核对；文档描述不能替代源代码和实车验证。

历史实施顺序（现已完成，不是当前待办）：

1. 以逐飞 MDK 模板建立终版工程，完成 LED、UART、PWM和定时器基础验证。
2. 先移植参考工程的纯算法 `pid` 与 `speed_loop_core`，使用固定输入验证。
3. 按终版 PCB 引脚重写 TB6612 和双编码器硬件适配层。
4. 架空车轮确认左右映射、正转计数符号和每圈计数，再启用速度闭环。
5. 速度闭环稳定后接入 OLED、IMU 和巡线外环；当前工程未引入 FreeRTOS。
6. 灰度巡线逻辑已在终版工程中独立开发完成；参考工程只提供速度闭环结构参考。

### 7.4 当前正式工程的 TB6612 底层

建立日期：2026-07-23。

- 已新增独立 `tb6612.c/.h`，并加入 Keil 工程。
- 当前引脚按终版原理图配置：
  - PWMA=PA12、AIN1=PB19、AIN2=PB17；
  - PWMB=PA13、BIN1=PA16、BIN2=PB24。
- PWM 使用 20kHz，占空比接口范围为 -10000～10000。
- `TB6612_OUTPUT_ENABLE=1`，供电、正反转、架空闭环和实车巡线均已验证。
- 上电保持 PWM=0；`1` 命令等待 3 秒后运行，`0` 立即停车，丢线搜索超时也会停车。

### 7.5 当前双编码器底层

- MOTOR1 编码器使用 PA25/PA14，MOTOR2 使用 PA26/PA27。
- PA26/PA27可以映射到硬件 TIMG8 正交编码器，但 PA25/PA14不能组成同一硬件正交通道。
- 为使两路计数口径一致，当前四根信号均采用 GPIO 双边沿中断和软件状态转移表解码。
- 当前为 A/B 两相四倍频有符号累计。
- 两路编码器已完成手动转动、正反转、每圈计数和闭环验证。
- 实车映射为左轮=MOTOR1(PA25/PA14)+TB6612 B，右轮=MOTOR2(PA26/PA27)+TB6612 A。
- 首次实测 MOTOR2 正交计数正常，向小车前进方向旋转时计数为负，反向为正。
- 旧映射下 MOTOR1 逐相诊断为监听PA24的通道始终为0、PA25持续产生边沿；交换电机后现象仍固定。重新核对原理图发现PA24本来就未作为MOTOR1编码器输入，实际信号是PA25/PA14，因此不能据此判定板卡故障。
- MOTOR2 逐相诊断结果为 A=4882、B=4881，两相匹配良好，软件正交解码正常。
- MOTOR1 软件解码使用 PA25/PA14，两相边沿和方向已验证。
- 速度层已按整车坐标统一反馈符号：左轮 MOTOR1 原始反馈乘 -1，右轮 MOTOR2 原始反馈乘 +1。
- 补充参考工程：`D:\robotmaster pj\RM PID\F401_PID 7.16\F401_PID-DIR\F401_PID-DIR` 是 STM32F401 + FreeRTOS + 四麦轮 + TB6612 的速度 PID 工程。可参考其100Hz采样、每轮独立PID、换向清积分和编码器转速换算结构。其编码器配置为11脉冲/电机轴转、四倍频、21.3:1减速比（理论937.2计数/输出轴转），与当前电机的11脉冲资料相符但减速比尚不能据此认定。工程中的STM32硬件层、麦轮运动学、Kp=35/Ki=10、单轮经验倍率及停止目标0.01均不得直接移植。
- 学长已同时提供该麦轮项目所用电机，必要时可将其作为当前 JGB680 的替换方案。若使用，应先确认实物型号、额定电压、减速比、额定/空载/堵转电流、输出转速、编码器定义、安装尺寸和输出轴规格；只有硬件条件一致后才可采用参考工程的21.3:1编码器参数，PID增益仍需重新整定。
- JGB680 十圈实测：MOTOR1 `-5252→19461`，每圈 `+2471.3`；MOTOR2 `386→-24371`，每圈 `-2475.7`，绝对值平均 `2473.5 count/rev`；正式程序按用户决定采用商家 `2450 count/rev`。

### 7.6 已验收实车固件的自适应巡线

- YB-MVX05 接线：A0=PB25、A1=PB18、A2=PB21、OUT=PB22，黑线有效为高电平。
- X1～X8 权重为 `-350,-250,-150,-50,50,150,250,350`。
- 基础循环 10 ms；速度 PI 周期 20 ms，KP=10、KI=1.0、KD=0，低通 `alpha=0.25`。
- 直线 55 RPM，普通弯最低 20 RPM；弯道立即减速、回正渐进提速，外轮不超过当前基础速度。
- 最外侧连续命中 20 ms 后进入急弯：外轮 28 RPM、内轮 -12 RPM；中心稳定 30 ms 后退出。
- 丢线后沿最后误差方向以外轮 22 RPM、内轮 -10 RPM 搜索 200 ms，超时停车。
- 用户已完成纯白底黑胶带实车验证；该结论不得外推到复杂背景。
- 2026-07-26 复杂地面复测确认旧数字灰度硬件不适用，准备切换具有 AO1～AO8 的模拟灰度模块。

## 8. 历年题型、2026器件清单与小车发展方向

### 8.1 规划依据

已分析资料目录：

`D:\xwechat_files\wxid_esykq0r0nuxe22_7930\msg\file\2026-07\电赛小车题汇总‘`

重点题型与共性能力：

| 资料 | 主要能力信号 |
|---|---|
| 2022 无线充电可循迹电动小车 | 巡线、充电结束自主启动、超级电容/能量管理、无线供能 |
| 2022 自动泊车 | 环境边界检测、倒车/侧方入库、精确停车、连续动作 |
| 2022 小车跟随行驶系统 | 0.3～1 m/s、车车通信、间距控制、计圈、分岔、停车标志 |
| 2023 空地协同消防 | 无线坐标、目标检测、路径规划、避障、执行器 |
| 2024 自动行驶小车 | MSPM0、开放路段定距离行驶、半圆弧巡线、节点声光、四圈稳定性 |
| 2025 简易自行瞄准装置 | 1～5圈快速巡线、摄像/目标识别、运动载体上的执行机构协同 |
| 2026 吉林轮式智能车 | 窄桥、阶梯、避障、抓取搬运、无线分段计时 |

2026器件清单包含电动小车、短距无线、无线图传、摄像头、光电与角度传感器、小型电磁铁、约1 cm钢球、小型电机及驱动、漆包线、磁环/磁芯、大功率器件、ADC/DAC和多类模拟器件。由此推断：

1. 摄像/目标识别、车载无线通信、光电检测具有较高准备价值。
2. 电磁铁与钢球组合明显指向吸取、搬运、释放或状态检测，应准备快拆执行模块。
3. 小电机与角度传感器适合预研通用单轴位置闭环，不应只做某一道题的专用机构。
4. 磁性器件、漆包线和功率/模拟器件提示无线供能或磁耦合方向，但应独立台架验证，不提前绑死小车结构。
5. 清单未出现的激光雷达、UWB、超声阵列、麦克纳姆轮、大型机械臂和复杂 SLAM 暂不作为基础车投入方向。

以上仅用于确定准备优先级，不代表清单内器件一定同时用于同一道题。

### 8.2 平台能力缺口

当前已经具备：双轮驱动、编码器计数、速度 PI、基础巡线、OLED、蜂鸣器、IMU 硬件与校准、HC-05通信。

当前主要缺口：

1. 没有经过实车验收的模拟灰度采集、逐路标定和复杂地面循迹。
2. 没有统一里程计、距离位置环、制动曲线和厘米级停车。
3. IMU 尚未进入正式控制链路，没有航向保持和定角转弯动作库。
4. 没有计圈、节点/标志事件和通用任务状态机。
5. HC-05 只有简单 `1/0` 命令，缺帧协议、校验、心跳和掉线处理。
6. 摄像头、电磁铁/钢球、小电机角度闭环尚未形成标准接口。
7. 当前 56:1、178 RPM、66 mm 车轮理论最高车速约 0.62 m/s，可能无法覆盖历史上 1 m/s 或多圈限时任务，需要实测并准备高速动力备选。
8. 底盘轮距、整车尺寸、重量、重心、满载最高速度和制动距离尚未完整记录。

### 8.3 总体发展原则

- 不再把项目仅作为“巡线车”调试，而是建设“精确运动底座 + 通用任务框架 + 可插拔感知/执行模块”。
- 控制层保持分层：硬件驱动 → 速度环 → 里程/航向/位置环 → 基础动作 → 事件与状态机 → 赛题流程。
- 赛题公布前优先开发可跨题复用的能力，专用机械结构和专用识别逻辑后置。
- 新模块必须快拆、独立供电评估、接口明确，并保留当前已验证基础功能的回退路径。

### 8.4 分阶段发展方向

#### P0：模拟灰度与复杂地面循迹

- 测量 AO1～AO8 全范围，确保 MSPM0 ADC 输入不超过 VDD。
- 完成八路 ADC、逐路黑白标定、归一化、模拟加权质心、可信度和异常检测。
- 建立普通线、丢线、宽线、双横线/标志等事件接口。
- 按 70→80→90→100 RPM 分级验证，不把目标参数当作实车结论。

#### P0：精确运动底座

- 测量并录入轮径、轮距、整车尺寸、重量、重心和传感器相对位置。
- 建立左右轮里程、平均路程、差分转角和分段清零。
- 增加距离位置环、加减速、制动和低速对点。
- 将 MPU-6500 加入正式控制，融合陀螺仪与编码器航向。
- 提供定距离直行、航向保持、定角转弯、倒车和停车动作。
- 目标验收：100 cm直行误差≤1 cm、90°误差≤2°、停车误差≤2 cm，连续10次无明显累计失控。

#### P1：通用任务框架

- 建立等待、校准、巡线、定距、转向、搜索、执行、返回、停车和故障状态。
- 支持任务编号、圈数和速度设置，OLED显示阶段、传感器和故障。
- 实现起终点、横线、双横线、计圈和分段计时。
- 增加编码器、传感器、欠压、通信和超时保护。

#### P1：2026清单内扩展

- 摄像头：标准 UART 目标数据协议、供电、支架、坐标标定和超时保护。
- 电磁铁/钢球：MOSFET、续流、独立功率路径、光电确认、温升测试和快拆支架。
- 小电机/角度传感器：通用单轴位置闭环。
- 短距无线：帧头、长度、类型、序号、校验、确认、心跳和掉线处理。
- 无线供能：独立台架预研谐振、功率驱动、整流稳压和检测。

#### P1：速度和机械能力

- 实测 RPM 到 m/s、满载最高速度、制动距离、温升和续航。
- 若现有 56:1 电机速度不足，准备低减速比编码电机，但先核对驱动电流和安装兼容。
- 评估窄桥、坡道和阶梯上的离地间隙、四点支撑、重心和驱动轮压力。

## 9. 仍未确定的信息

- 底盘实际轮距、整车长宽高、重量、重心和全部模块位置；
- 新模拟灰度 AO1～AO8 的实际电压范围、ADC映射和安装高度；
- 满载最高稳定速度、制动距离、温升和续航；
- 现有动力是否需要低减速比高速备选；
- 比赛赛道和最终题目要求。

上述机械和性能数据是精确运动控制的输入，不再推迟到赛题公布后才测量；比赛题目本身仍待正式发布。
